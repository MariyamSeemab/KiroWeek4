[phases.setup]
nixPkgs = ["nodejs_22", "python3", "cairo", "pango", "libjpeg", "giflib", "librsvg"]

[phases.install]
cmds = ["cd backend && npm ci"]

[phases.build]
cmds = ["cd backend && npm run build"]

[start]
cmd = "cd backend && npm start"